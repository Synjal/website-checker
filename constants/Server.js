// SERVER MAIN PATH
export const server = 'https://website-checker.boreales-creations.fr';

// AUTH PATHS
export const auth = server + '/auth';
export const register = server + '/api/users';

// RESSOURCES PATHS
export const websites = server + '/api/websites';
export const users = server + '/api/users';

